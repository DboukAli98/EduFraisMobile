import ParentLoyaltyLedgerScreen from '../../src/screens/parent/ParentLoyaltyLedgerScreen';

// Shared between parent and agent — both use the same paged ledger view.
export default ParentLoyaltyLedgerScreen;
